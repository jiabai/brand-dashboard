## Summary
进一步扩展了 Vite 开发服务器的 `allowedHosts` 配置，以支持更多子域名。

## Code Highlights
- [vite.config.js](file:///d:/Github/brand-dashboard/web/vite.config.js): 在 `server.allowedHosts` 中添加了 `www.rushlink.click`。

## Self-Tests
- 确认 `vite.config.js` 语法正确。
- 确认包含所有必要的 host 配置。
