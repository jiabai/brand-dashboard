## Summary
更新了 `docker-compose.dev.yml` 中的环境变量配置。将 `VITE_DEFAULT_USER_ID` 重命名为 `VITE_DEFAULT_TENANT_KEY`，以更准确地反映其在前端代码中的语义（作为租户 Key 使用）。同时更新了 `VITE_DEFAULT_TENANT_KEY` 和 `VITE_DEFAULT_JOB_ID` 的默认值为最新的可用数据，并同步更新了相关配置文件和文档。

## Code Highlights
- **[docker-compose.dev.yml](file:///d:/Github/brand-dashboard/docker-compose.dev.yml)**: 重命名环境变量并更新默认业务参数。
- **[web/.env](file:///d:/Github/brand-dashboard/web/.env)**: 同步更新前端本地环境变量模板。
- **[web/src/config.js](file:///d:/Github/brand-dashboard/web/src/config.js)**: 修改配置逻辑，使其优先从环境变量读取 `VITE_DEFAULT_TENANT_KEY` 等参数。
- **[README.md](file:///d:/Github/brand-dashboard/README.md)** & **[web/README.md](file:///d:/Github/brand-dashboard/web/README.md)**: 更新环境配置说明文档。

## Self-Tests
- 检查了代码库中所有 `VITE_DEFAULT_USER_ID` 的引用，确认已全部清理。
- 验证了 `web/src/config.js` 中的 fallback 逻辑。
- 更新了 `tasks.md` 记录。
