## Summary
- 重命名后端模块为 `api`，补充依赖与 Ruff 配置，并在预提交中加入后端检查。
- 将前端项目迁移到 `web/` 目录，更新 Vite/Tailwind 配置及 Husky 路径，同时梳理 README 与部署文档。
- 添加后端 Dockerfile 与 `.dockerignore`，并在任务清单中标记文档整理已完成。

## Code Highlights
- `api/main.py` 及路由改为使用 `api` 包引用，新增 `api/requirements*.txt` 与 `api/pyproject.toml` 以支撑 Ruff 检查。
- `.husky/pre-commit` 增加 `ruff check api`，并改为通过 `npm --prefix web` 调用前端 lint/test；`commit-msg` 依赖 web 下的 commitlint。
- 前端配置更新（`web/vite.config.js` 输出目录与 `VITE_API_TARGET` 支持、`web/tailwind.config.js` 内容匹配 `web/src`）并调整 `.gitignore`、AGENTS/README/DEPLOYMENT 说明。
- 新增根目录 `Dockerfile` 与 `.dockerignore`，统一构建后端镜像并隔离前端文件；补充 `docker-compose.yml` 以便本地一键启动前后端，新增 `mysql` 服务并使用环境变量配置数据库连接，同时提供 `web/Dockerfile` + `web/nginx.conf` 产出 Nginx 前端镜像。

## Self-Tests
- 未运行自动化测试；仅进行长行脚本检查与配置审阅，后续需在本地安装依赖后验证 `ruff check` 与前端构建。
