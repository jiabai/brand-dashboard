# 2026-06-09 Daily Log

## API 服务器启动条件确认
- 验证了 API 服务器的完整启动条件链：
  - Python 3.13.12 ✅（满足 >=3.11 要求）
  - uv 0.11.3 ✅
  - 依赖已通过 .venv 安装（fastapi, sqlalchemy, uvicorn, pydantic, pymysql 等均 OK）✅
  - api/.env 配置为 SQLite 模式 ✅
  - SQLite 数据库文件存在 `data/geo_csv/geo_migrated.db`（16MB）✅
  - 表结构已初始化（启动日志显示 "Database already has tables"）✅
  - 定时任务调度器正常启动 ✅
  - 健康检查接口返回 `{"status":"healthy","service":"brand-analysis-api","version":"0.1.0"}` ✅
- 正确启动方式：从项目根目录运行 `uvicorn api.main:app --reload` 或 `api/.venv/Scripts/uvicorn api.main:app`
- 注意：`python api/main.py` 会因模块导入路径问题报错，需使用 uvicorn 方式启动

## 前端启动条件确认
- 确认 web/ 前端（React 18 + Vite 5 + Tailwind v4）具备完整启动条件：
  - package.json 完好，依赖齐全（node_modules 已安装）✅
  - .env 配置完整：VITE_API_TARGET=http://localhost:8000, VITE_USE_MOCK=false, VITE_DEV_PORT=3000 ✅
  - vite.config.js 配置完整（代理、路径别名、chunk 分拆）✅
  - 入口文件 index.html → main.jsx → App.jsx 正常 ✅
  - 构建产物 dist/ 已存在（旧构建）✅
- 验证启动成功：`npm run dev` → Vite v5.4.21 ready in 810ms → http://localhost:3000/ ✅
- 项目使用 JSX 而非 TSX，无 tsconfig 是预期行为

## 排查登录失败 & 重置密码
- `lantianye@163.com` 在 `geo_migrated.db` 中存在，创建于 2026-05-20，状态 active
- 密码哈希格式正确但非 `admin123`，已重置为 `admin123` ✅
- 重置后登录验证通过，返回 accessToken + tenant 信息

## 前端导航漏洞发现
- 项目路由分两层：menuSection='main'（projects, accounts）和 'legacy'（home, trend 等）
- 侧边栏只渲染 'main' 路由（getSidebarMenuRoutes）, 首页/dashboard 路由不可见
- 项目页面（ProjectListPage/ProjectDetailPage）没有"返回首页/dashboard"按钮
- Breadcrumb 是纯文本 `<span>`，不可点击导航
- home 路由需 jobId 参数，但项目列表页没有 jobId，导致无法构建 dashboard URL
