set -euo pipefail

COMMIT_MSG_FILE="$1"

if [ -x "./node_modules/.bin/commitlint" ]; then
  node ./node_modules/.bin/commitlint --edit "$COMMIT_MSG_FILE"
else
  echo "[commitlint] commitlint is not installed; skipping commit message check." >&2
fi

