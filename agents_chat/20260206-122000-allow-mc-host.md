## Summary
允许 Vite 开发服务器通过 `mc.rushlink.click` 域名访问，解决了 host 限制导致的 Blocked request 错误。

## Code Highlights
- [vite.config.js](file:///d:/Github/brand-dashboard/web/vite.config.js): 在 `server.allowedHosts` 中补充了 `mc.rushlink.click`。

## Self-Tests
- 确认 `vite.config.js` 语法正确。
- 准备提交以解决用户反馈的域名拦截问题。
